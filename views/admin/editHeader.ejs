<%- include('a-header'); -%>

<% if(error == "" || error == null){%>

  <% }else{ %>
  <span class="error">
    <p><%=error%></p> <i id="removeFlash" class="bi bi-x-lg"></i>
  </span>
<% } %>

<% if(info == "" ||info == null){%>

  <% }else{%>
  <span class="info">
    <p><%=info%> </p> <i id="removeFlash" class="bi bi-x-lg"></i>
  </span>
<%}%>
<div class="body-con" style="flex-direction: column; ">
<div class="formDiv">
 
  <form id="formfile" action="/admin/editheader/<%=action%>" method="post" >
    
    <h4>edit  code here</h4>
    <button type="submit">Seve</button>
    <input type="hidden" value="<%=file%>" id="hideinput">
    <textarea name='fileData' id="htmleditor"></textarea>
    
  </form>

</div></div>

<%- include('a-footer'); -%>